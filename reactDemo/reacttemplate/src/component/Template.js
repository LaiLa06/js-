import React from 'react'
import PropsType from 'prop-types'
class Template extends React.Component{
    static defaultProps = {};  // 设置默认属性
    static propTypes = {}; // 设置属性规则
    constructor(props){
        super(props);

    }
    render(){
        return <section>

        </section>
    }
}

export default Template;